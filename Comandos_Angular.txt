**actualizar node
https://askubuntu.com/questions/426750/how-can-i-update-my-nodejs-to-the-latest-version

sudo npm cache clean -f
sudo npm install -g n
sudo n stable

Instalar Angular
npm install -g @angular/cli  //instalación de Angular cli

Generamos nuevos proyectos de Angular
ng new aprendiendo-angular

ng serve (iniciar angular)
ng generate component cursos (generar un componente)
ng g component externo (generar un componente)

** error TS2307: Cannot find module 'rxjs-compat/Observable'
npm install --save rxjs-compat

**npm audit fix (fix vulnerabilities)



